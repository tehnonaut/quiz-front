import { QuizDashboard } from '@/components/quiz-dashboard';

export const metadata = {
	title: 'Quiz Tools - Dashboard',
	description: 'Quiz dashboard',
};

export default function Page() {
	return <QuizDashboard />;
}
